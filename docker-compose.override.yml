# Docker Compose override file for service-specific overrides
# The common YAML anchors are now defined in docker-compose.yml

version: '3.8'

# Service-specific overrides that supplement the main configuration
services: {}