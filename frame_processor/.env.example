# Frame Processor Environment Configuration
# Copy this to .env and update with your values

# ========== ESSENTIAL CONFIGURATION ==========

# API Keys (Required for object identification)
SERPAPI_API_KEY=your_serpapi_key_here
PPLX_API_KEY=your_perplexity_key_here

# Google Cloud Storage (Required for Lens API)
GCS_BUCKET_NAME=worldsystem-frame-processor

# ========== PERFORMANCE TUNING ==========

# Configuration Profile - Controls multiple settings at once
# Options: performance (fast/lower quality), balanced (default), quality (slower/better)
CONFIG_PROFILE=balanced

# Or override individual settings:
# TARGET_FPS=15              # Target frames per second
# SAM2_MODEL_SIZE=small      # Model size: tiny, small, base, large
# PROCESSING_RESOLUTION=720   # Max resolution: 480, 720, 1080

# ========== FEATURE FLAGS ==========

# Enable/disable major features
VIDEO_MODE=true          # Use SAM2 video tracking (recommended)
USE_SERPAPI=true         # Enable Google Lens identification
USE_GCS=true            # Enable cloud storage for API
RERUN_ENABLED=true      # Enable real-time visualization

# ========== ADVANCED SETTINGS ==========
# Usually don't need to change these

# Video Processing
# GRID_PROMPT_DENSITY=16     # Object discovery density
# REPROMPT_INTERVAL=60       # Frames between re-detection
# MIN_OBJECT_AREA=1000       # Minimum object size in pixels

# Stream Management
# STREAM_STALE_TIMEOUT_SECONDS=30    # Mark stream stale after
# STREAM_CLEANUP_TIMEOUT_SECONDS=120  # Remove stream after

# Batch Processing
# LENS_BATCH_SIZE=10         # Objects per batch
# LENS_BATCH_WAIT_MS=500     # Max wait time for batch

# GPU Memory
# ENABLE_DYNAMIC_MODEL_SWITCHING=true  # Auto-switch models on OOM
# MODEL_SWITCH_THRESHOLD_MB=3000       # Free memory threshold