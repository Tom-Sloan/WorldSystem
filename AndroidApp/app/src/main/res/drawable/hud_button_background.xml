<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Outer glow -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="4dp"/>
                    <padding android:left="2dp" android:top="2dp" android:right="2dp" android:bottom="2dp"/>
                    <stroke android:width="2dp" android:color="@color/hud_glow_intense"/>
                    <solid android:color="@color/hud_cyan_20"/>
                </shape>
            </item>
            <!-- Inner border -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="4dp"/>
                    <stroke android:width="1dp" android:color="@color/hud_primary_cyan"/>
                    <solid android:color="@color/hud_cyan_10"/>
                </shape>
            </item>
        </layer-list>
    </item>
    
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="4dp"/>
            <stroke android:width="1dp" android:color="@color/hud_border"/>
            <solid android:color="@android:color/transparent"/>
        </shape>
    </item>
</selector>